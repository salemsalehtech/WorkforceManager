using Xunit;

namespace WorkforceManager.Tests
{
    /// <summary>
    /// اختبارات النسخ الاحتياطي بتشتغل **لوحدها مش بالتوازي مع غيرها**.
    ///
    /// السبب: الاسترجاع بينادي SqliteConnection.ClearAllPools()، وده
    /// **عام على العملية كلها** — بيقفل الاتصالات المتجمّعة لكل قاعدة
    /// بيانات في العملية، بما فيها قواعد الاختبارات التانية اللي شغالة
    /// في نفس اللحظة. النتيجة كانت اختبار بيقع مرة كل كام تشغيلة من غير
    /// أي علاقة بالكود اللي بيختبره — وده أسوأ نوع من الأعطال لأنه
    /// بيعلّم الناس تتجاهل الأحمر.
    ///
    /// القفل ده مش ممكن يتشال من الاسترجاع: إحنا بنستبدل ملف قاعدة
    /// البيانات نفسه. فالعزل هنا هو الحل الصح.
    /// </summary>
    [CollectionDefinition("Backup", DisableParallelization = true)]
    public class BackupCollection { }
}
