<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- الإعدادات المشتركة (ImplicitUsings / Nullable / اللغات) في Directory.Build.props -->
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>WorkforceManager.Core</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- نحتاج حزمة EF Core الأساسية هنا فقط لأجل الـ [Index] attribute على النماذج -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="8.0.29" />
  </ItemGroup>

</Project>
