<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- الإعدادات المشتركة (ImplicitUsings / Nullable / اللغات) في Directory.Build.props -->
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>WorkforceManager.Core</RootNamespace>
  </PropertyGroup>

  <!--
    كانت هنا PackageReference لحزمة Microsoft.EntityFrameworkCore الكاملة
    لمجرد الـ [Index] attribute على النماذج — عكس مبدأ "Core مالوش أي
    اعتماد على EF Core أو WPF" في معمارية المشروع (شوف CLAUDE.md). كل
    الفهارس اتنقلت لـ fluent API في AppDbContext.OnModelCreating
    (WorkforceManager.Data)، فـ Core بقى POCO models فعلًا من غير أي
    Package خارجي خالص.
  -->

</Project>
