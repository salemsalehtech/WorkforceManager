namespace WorkforceManager.Core.Enums
{
    /// <summary>
    /// التقييم ده جه منين — بيتعرض للمستخدم عشان يفهم الرقم اتحدد إزاي.
    ///
    /// من غير التفرقة دي، المستخدم بيشوف رقم اتغيّر لوحده ومش عارف
    /// النظام غيّره ولا حد عدّله، فبيفقد الثقة في الرقم كله.
    /// </summary>
    public enum SkillRatingSource
    {
        /// <summary>حد حدّده بإيده — النظام مبيلمسوش لحد ما يبقى فيه إنتاج كفاية</summary>
        Manual = 1,

        /// <summary>النظام حسبه من إنتاج العامل الفعلي على المرحلة دي</summary>
        Auto = 2
    }
}
